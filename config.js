(function(w) {
    var config = window._conf_ = {
        /**
         * ログを関数を使用する場合true. 
         */
        enabledLog:true,
        /**
         * パッケージパスのエイリアスを設定します. 
         */
        jsmlAlias:{
            root:"./"
        }
    };
})(window);