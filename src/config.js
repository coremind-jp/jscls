;(function(g)
{
    var config = g._conf_ = {
        /* ログを関数を使用する場合true. */
        enabledLog:true,
        /* パッケージパスのエイリアスを設定します. */
        jsmlAlias:{
            cls:"./"
        }
    };
})(this.global || this.window);